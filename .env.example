# Copy to .env and fill in. Never commit real credentials.
# Must be a standard postgresql:// URL (direct or pooled TCP connection).
# `prisma+postgres://` Accelerate URLs are NOT supported by Prisma 5.22.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
